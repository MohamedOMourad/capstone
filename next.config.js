/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  swcMinify: true,
  images: {
    domains: ['kjwldcclontmioflpfvx.supabase.co']
  }
}

module.exports = nextConfig
